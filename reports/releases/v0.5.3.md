# VoxelMill 0.5.3 alpha verification

Verification date: 2026-09-20. This record distinguishes the local acceptance
artifact from the tag workflow's published artifact.

## Implementation

Main support pillars remain. Branches descend at 45° from full-width shoulders,
using 15 mm vertical spacing and a 30 mm maximum diagonal length by default.
Neighbour reach and primary support spacing remain separate. Primary
part-to-part routing requires explicit enabling; brace networks always require
support-only grounding. Geometry tests cover shared junctions, unequal heights,
elbows, duplicate suppression, thickness clearance, multiple model parts,
complete feet, boundaries, tree supports, cancellation and work limits.

Prepared-mesh tests inspect exact reopened connectivity and decoded layers.
Grid bases receive a precision-bounded simplification before STL serialization
to prevent collapsed faces. Separate regressions retain honest drainage
rejection where low braces narrow an otherwise open channel.

## Local Linux AppImage

Built with the independent CPU-only native extension, then tested with
`scripts/appimage_acceptance.py`. The staged application Python files matched
the source tree byte for byte; the runtime imported the extracted package and
reported CUDA compiled=false.

Artifact: `output/appimage-053/VoxelMill-0.5.3-linux-x86_64.AppImage`.

SHA-256:
`0fb365c9cb0f5f7d2b2475dc4726c1dea66e765f2eb00f7e7b9eb263c9dd71bc`.

- Startup/version, model inspection and loading: passed.
- Settings, regeneration, project save/reopen and layer preview: passed.
- Actual VTK framebuffer: 35,601 surface pixels; 28 contacts and 28 braces.
- Reopened prepared mesh: 29,334 triangles, one exact component, minimum Z=0.
- Full prepared raster: zero floating islands; support routes passed.
- GOO slicing: 268,383 bytes; every decoded source pixel verified.
- Standalone decoded validation: passed; connectivity, overlap, growth and
  enclosed-void checks passed, with the reported transient-trap warning retained.

Preparation intentionally disables drainage/void analysis for this harness,
so its export is explicitly warned; slicing and standalone decoded validation
retain their own checks. This does not bypass geometry, route or pixel checks.

Machine-readable evidence is in
`output/appimage-053/acceptance-final/acceptance.json` and its adjacent reports.

## Release gates

Before committing, `VOXELMILL_SAMPLES=1 .venv/bin/python -m pytest -q -ra`
passed **1,173 tests**, with 8 skips, in 240.09 seconds. This includes the
available original-model tests and actual-render GUI tests. Skips: one missing
immutable reference GOO, five FreeCAD-dependent tests, and the unavailable
zsh/fish completion tests. Local AppImage acceptance also passed before commit.

The manually dispatched release workflow passed all four platform jobs on
commit `ad1186ceb5ae6ef56d4fc3a1d51815b56537685c`: [run 35522204740](https://github.com/chememjc/VoxelMill/actions/runs/35522204740)
(Linux AppImage, macOS ARM, macOS Intel, and Windows). Tag `v0.5.3` was then
created and pushed at that exact commit. The tag workflow and publication
completed successfully in [run 35538048868](https://github.com/chememjc/VoxelMill/actions/runs/35538048868),
including Linux, macOS ARM, macOS Intel, Windows, and publication.

## Published Linux AppImage

The published prerelease is [v0.5.3](https://github.com/chememjc/VoxelMill/releases/tag/v0.5.3),
with `prerelease=true`, `draft=false`, published 2026-09-20T21:24:42Z. The
downloaded AppImage acceptance passed using
`output/published-053/acceptance/acceptance.json`.

Artifact size is 420,485,624 bytes and its SHA-256 is
`9006e1040a247a88875b720f7393baf7046b3eb80df74fd5b3cd14395d1065c1`, matching
the GitHub artifact digest. The runtime is CPU-only. Startup, settings save and
reopen, support regeneration, project save/reopen, layer preview, actual VTK
rendering (35,593 surface pixels), and model loading passed. The reopened
prepared mesh has 29,322 triangles, one component, and minimum Z=0; the raster
has zero floating islands and contains 28 contacts and 28 braces. GOO output
was 268,184 bytes with decoded-pixel parity passing. Standalone decoded
validation passed all checks, including transient-trap checks.

The published acceptance preparation warning is limited to the harness's
intentional disabling of drainage/void analysis. It does not indicate a
published validation failure; the published standalone checks all passed.

The additional benchmark comparison used the existing historical baseline.
Five of the six benchmark timings improved. `inspect` measured 0.21 s versus
0.18 s in the historical baseline, remaining within its 50% tolerance. The
`slice_small` peak RSS was 220,348,416 bytes versus 172,560,384 bytes (27.7%
above baseline), exceeding its 15% memory threshold. This is recorded
separately from the passing functional suite and both AppImage acceptance
runs; the baseline was not rewritten to conceal the result.

## Coverage limits

Linux acceptance uses Xvfb and actual VTK rendering. macOS and Windows runtime
acceptance and physical printer trials are outside this Linux verification;
their portable builds and CLI smoke checks are required in GitHub Actions.
